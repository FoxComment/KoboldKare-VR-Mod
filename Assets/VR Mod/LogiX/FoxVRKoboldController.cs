using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FoxVRKoboldController : MonoBehaviour
{
    [Header("All the VR stuff will be moved in here later")]
    [SerializeField] string oneDay;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
